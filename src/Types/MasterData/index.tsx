export interface masterVarianType {
    "id": string,
    "varian": string,
    "created_at": string
}
[];
export interface masterRakType {
    "id": string,
    "nomor_rak": string,
    "created_at": string
}
[];

export interface masterLokasiType {
    id: string,
    nama_lokasi: string,
    created_at: string,
}
[];